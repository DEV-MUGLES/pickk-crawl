import OptionCralwer from './Crawler';
import { OptionResult } from 'types/option';

export const _theknitcompanycom = (url: string, html: string): OptionResult => {
  return new OptionCralwer(url, html).cafe24().result;
};

export const _modninecom = (url: string, html: string): OptionResult => {
  return new OptionCralwer(url, html).cafe24().result;
};

export const _garmentlablecom = (url: string, html: string): OptionResult => {
  return new OptionCralwer(url, html).cafe24().result;
};

export const _bananafitcokr = (url: string, html: string): OptionResult => {
  return new OptionCralwer(url, html).cafe24().result;
};
